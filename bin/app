#!/usr/bin/env node

import { OrderFlowDashboard, createDependencies } from "../dist/index.js";
 
const dependencies = createDependencies();
const processor = new OrderFlowDashboard(dependencies);
processor
    .startDashboard()
    .catch((err) => console.error("Failed to start dashboard:", err));
