#!/usr/bin/env node

import { OrderFlowDashboard } from "../dist/index.js";
 
const processor = new OrderFlowDashboard();
processor
    .startDashboard()
    .catch((err) => console.error("Failed to start dashboard:", err));
