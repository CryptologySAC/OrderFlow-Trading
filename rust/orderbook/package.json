{
    "name": "orderbook-native",
    "version": "0.1.0",
    "description": "High-performance Rust order book implementation",
    "main": "native/index.node",
    "scripts": {
        "build": "cargo build --release && npm run copy-native",
        "build-debug": "cargo build && npm run copy-native",
        "copy-native": "node scripts/copy-native.js",
        "test": "cargo test",
        "clean": "cargo clean && rm -rf native/"
    },
    "keywords": [
        "orderbook",
        "trading",
        "rust",
        "performance"
    ],
    "author": "OrderFlow Trading",
    "license": "MIT",
    "engines": {
        "node": ">=16.0.0"
    }
}
